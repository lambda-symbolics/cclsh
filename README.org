#+TITLE: cclsh
#+AUTHOR: Lukáš Hozda

A system shell running inside Clozure CL. Ordinary Unix commands and Common
Lisp forms share one live image, so the boundary between shell use and Lisp is
small and explicit.

cclsh targets Linux x86-64 and has experimental NetBSD ARMv7 support, tested
on NetBSD 11.0/earmv7hf against a downstream CCL port. It is not a POSIX shell:
command strings, scripts, pipelines and redirection use cclsh syntax. Try it as
an ordinary shell before considering login-shell use.

* Try it

On Linux with Nix flakes enabled:

#+begin_src sh
nix run github:luciusmagn/cclsh -- --version
nix run github:luciusmagn/cclsh
#+end_src

Then type these lines inside cclsh:

#+begin_src text
pwd
(+ 1 2)
echo "Lisp result:" (+ 20 22)
help
#+end_src

The first line runs a normal command, the second evaluates Lisp, and the third
substitutes a Lisp result into a command.

* Install

The Nix flake is the recommended Linux package:

#+begin_src sh
nix profile install github:luciusmagn/cclsh
cclsh --version
#+end_src

From a checkout on Linux, use =nix run .= or =nix profile install .#cclsh=.
NetBSD 11.0/earmv7hf uses the source build and experimental downstream CCL port
described in the installation guide.

See [[file:docs/installation.org][Installing cclsh]] for Quicklisp state, source
builds, local installation and the guarded shared login-shell procedure.

* Shell and Lisp

A submitted line is interpreted by its shape:

- A line beginning with =(= is evaluated as Common Lisp in the =cclsh-user=
  package.
- Any other line starts in command mode and resolves a builtin or executable.
- Parenthesized Lisp inside a command is evaluated and substituted into its
  arguments.

Pipelines, sequencing and redirection are Lisp forms rather than sh operators:

#+begin_src lisp
(pipe (ls "-la") (grep "lisp"))
(pipe (make) (to "build.log"))
(capture (git "rev-parse" "--short" "HEAD"))
#+end_src

The same helpers are available to CCL programs after loading the =cclsh=
system. Commands can also be defined in Lisp with =defcommand= and used from
both command mode and Lisp.

* Highlights

- Clinedi line editing with history search, completion and highlighting
- foreground process groups, Ctrl-C handling and =jobs=/=fg=/=bg=/=disown=
- startup configuration in =~/.config/cclsh/startup.lisp=
- command strings, scripts, shebang scripts and piped input
- optional zoxide integration with =z= and =zi=
- optional experimental =cclsh-fast= prewarmed interactive launcher on Linux

* Documentation

- [[file:docs/guide.org][User guide]]: syntax, substitution, pipelines, custom
  commands, job control, completion, configuration and scripting
- [[file:docs/installation.org][Installation]]: Nix, source builds and shared
  login-shell deployment
- [[file:docs/installation.org::*Experimental prewarmed launcher][Prewarmed launcher]]:
  opt-in daemon setup, fallback behavior and current limits
- [[file:examples/startup.lisp][Example startup file]]
- [[file:docs/README.org][Documentation index]]

The built-in manual is available as =help= inside cclsh, or as =cclsh help=
from another shell. Use =cclsh help SECTION= for a specific topic.

* Development

Stock CCL 1.13 is sufficient for Linux regression checks and unsaved REPL
development:

#+begin_src sh
scripts/check
#+end_src

NetBSD 11.0/earmv7hf requires the matched kernel and image from the experimental
downstream CCL port recorded in the installation guide:

#+begin_src sh
make check CCL=../ccl/narmcl CCL_IMAGE=../ccl/narmcl.image
#+end_src

The =CCL= and =CCL_IMAGE= make variables select the kernel and matching image
used by checks when the platform defaults are not appropriate.

Standalone image builds require the downstream CCL changes documented in the
[[file:docs/installation.org][installation guide]].

* License

cclsh's original code and documentation are released under the ISC license.
See [[file:LICENSE][LICENSE]]. The CCL-derived patch mirrors under =patches/=
remain under the Apache License 2.0; see [[file:patches/LICENSE][their license]]
and [[file:patches/README.org][provenance ledger]].
