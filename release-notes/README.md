# Release notes

Before creating a version tag, add `v<version>.md` in this directory.  Write
concise user-facing patch notes in Markdown; the release workflow uses the
file verbatim as the GitHub release body.

Published releases are historical records.  Do not create note files merely to
retrofit notes into an existing release.
