# CCLSH 1.6.0

- Add the `scripts/cclsh-sbcl` source launcher for SBCL on macOS and Linux.
- Keep CCL's saved-image, release archive and optional `cclsh-fast` paths intact.
- Document both CCL and SBCL installation paths, including Quicklisp and source dependencies.
